BootStrap: docker
From: ubuntu:xenial
IncludeCmd: yes

%post
	# Get the system ready
	sed -i 's/main/main restricted universe/g' /etc/apt/sources.list
	apt-get update
	apt-get install -y apt-utils bash build-essential curl gcc wget nano dnsutils net-tools dsniff iproute2 tcpdump 
	apt-get clean
